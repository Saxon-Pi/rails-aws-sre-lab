<h1>Tasks</h1>

<p>
  <a href="/tasks/new">New Task</a>
</p>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <strong><%= task.title %></strong>
      - <%= task.description %>
      [<%= task.status %>]
    </li>
  <% end %>
</ul>
